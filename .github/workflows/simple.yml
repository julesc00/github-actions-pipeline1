name: Shell Commands

on: [push]

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: Echo a string
        run: echo "Hello world!"

      - name: Multiple scripts
        run: |
          node -v
          python -v
          npm -v